# enable continuous time-sync to public NTP servers (via systemd)
config_time_enable_sync: true

# primary/fallback NTP servers (if config_time_enable_sync enabled)
# Note: NTP servers are classified in hierarchy.
#   - Description: https://wiki.archlinux.org/index.php/Network_Time_Protocol_daemon#Connection_to_NTP_servers
#   - Server List: https://www.ntppool.org/en/
config_time_primary_ntp_servers: "0.pool.ntp.org 1.pool.ntp.org 2.pool.ntp.org 3.pool.ntp.org"
config_time_fallback_ntp_servers: "0.arch.pool.ntp.org 1.arch.pool.ntp.org 2.arch.pool.ntp.org 3.arch.pool.ntp.org"

# config file for NTP server list (if config_time_enable_sync is enabled)
config_time_timesyncd_conf_file: "/etc/systemd/timesyncd.conf"

# geo-IP API uri to retrieve timezone string (if config_time_zone def to 'auto')
config_time_geo_ip_uri: "https://ipapi.co/timezone"

