# config file for NTP server list (if cfg_time_enable_continuous_sync_to_ntp_servers is enabled)
cfg_time_freebsd_ntp_conf_filepath: "/etc/ntp.conf"

# config file for host info, network info, and services to start at boot
cfg_time_rc_conf_filepath: "/etc/rc.conf"

